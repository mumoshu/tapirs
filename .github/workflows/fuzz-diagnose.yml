name: Fuzz Diagnose

on:
  push:
    branches: [ main ]
    paths:
      - 'src/**'
      - 'scripts/fuzz-diagnose.sh'
      - 'Cargo.toml'
      - 'Cargo.lock'
  pull_request:
    branches: [ main ]
    paths:
      - 'src/**'
      - 'scripts/fuzz-diagnose.sh'
      - 'Cargo.toml'
      - 'Cargo.lock'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  fuzz-diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.93.0
      - name: Run fuzz-diagnose
        run: make ci/fuzz-diagnose
      - name: Collect logs
        if: failure()
        run: |
          mkdir -p /tmp/ci-logs
          cp -r /tmp/fuzz-diagnose-*/ /tmp/ci-logs/ 2>/dev/null || true
      - name: Upload logs
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: fuzz-diagnose-logs
          path: /tmp/ci-logs/
          retention-days: 14
